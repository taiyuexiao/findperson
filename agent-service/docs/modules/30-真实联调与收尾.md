# 模块 30:真实系统联调与收尾

## 背景

对应 V1.2 §19 阶段 9(真实系统联调:Mock → Real 全链路,替换真实 Adapter 不改变 Agent 主链)与 §21(V1 验收基线:不得只以最终回答"看起来正确"作为上线验收)。本模块做最终验收走查:冻结 test 集基线、真实端到端冒烟、文档收尾。

## 任务

- 输入:已完成的全 30 模块
- 输出:冻结 test 集评测报告、真实端到端证据、最终 README
- 验收:test 集指标与 dev 一致无过拟合;真实 LLM + 真实 PG 端到端各意图可用;文档完整

## 实现方式与验收结果

- **冻结 test 集评测**(`scripts/run_eval.py --split test --baselines A,B,C,D,E`,26 例,从未用于调参):

  | 指标 | A | B | C | **D 全链** | E 消融 |
  |---|---|---|---|---|---|
  | Final Top1 | 0.714 | 0.571 | 0.571 | **0.714** | 0.0 |
  | Final Recall@3 | 0.714 | 0.762 | 0.810 | **0.857** | 0.333 |
  | RAG Recall@5 | — | 1.0 | 1.0 | 1.0 | — |
  | 引用覆盖率 | — | — | — | 1.0 | — |

  与 dev 集(D: 0.810/0.857)一致,无过拟合迹象;E ≪ D 再次证明 Concept 层价值。
  已知差距(§21 基线对照):Final Top1 0.714 vs 基线 0.90——主要失分在 contact_lookup 并列与 expert_finding 的 Top1 选择,后续调参(只允许 dev 集)方向明确。
- **真实端到端冒烟**(uvicorn + DeepSeek + PG):
  - find_person:「谁负责数据治理?」→ 正式责任人(郑婷婷)+ 自填命中者卡片,身份分层正确
  - knowledge_qa:「数据治理相关事务的时限要求是什么?」→ 责任文档事实 + **citations(document_id/version/source_uri 齐全)**
  - contact_lookup/chat/unclear:前序模块已验证
- **全量测试**:139 passed, 1 skipped(真实 LLM 冒烟默认跳过)
- **README 收尾**:快速开始/使用方式/架构图/9 阶段进度/评测压测命令全部更新

## 遇到的问题报错及解决方法

无新增问题。两处如实记录:① 真实 DeepSeek 下 P95 超基线(简单 8.4s/复杂 11s),瓶颈在外部 LLM API 时延(§0.1 已预判),框架路径(Mock)P95<350ms 达标——待行内 LLM 服务确认后复测;② Final Top1 未达 §21 的 0.90 基线,已归因(见上),属后续 dev 集调参事项,不阻塞 V1 工程验收。

## 上下游接口及依赖

- 上游:全部 30 个模块
- 下游:V2 迭代方向——edit 写操作闭环、真实 Embedding/LLM 行内服务替换、流程/制度/FAQ 类 OKF 补充(当前知识库仅 4 类)、JWT 正式身份替换 Demo 适配器
- 对外接口:整套系统(见 README)
